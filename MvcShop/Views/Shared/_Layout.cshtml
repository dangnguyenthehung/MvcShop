@model MvcShop.Models.LayoutViewModel
@{
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}
<script>
    var AppUrlSettings = {
        AddToCartUrl: '@Url.Action("AddToCart","GioHang")',
        RemoveFromCartUrl: '@Url.Action("RemoveFromCart", "GioHang")',
        UpdateCartQuantityUrl: '@Url.Action("UpdateCartQuantity", "GioHang")',
        AddToCompareUrl: '@Url.Action("AddToCompare", "SoSanh")'
    }
</script>
    <div class="container body-content">
        @Html.Action("Slider")
        <section>
            <div class="container">
                <div class="row">
                    @{ Html.RenderPartial("SideMenu",Model);}
                    @RenderBody()
                </div>
            </div>
        </section> 
        @Html.Partial("CallButton")
        
        
    </div>

